NEXT_PUBLIC_SITE_URL=http://localhost:3000
NEXT_PUBLIC_DISCORD_URL=https://discord.gg/KY2dRw8Yh4

# Optional: force Retake #1 host (otherwise local/dev → staging 129.159.232.212).
# NEXT_PUBLIC_RETAKE_HOST=129.159.232.212

# --- Live server status (server-only) ---
# Leave empty locally to A2S-query without a DB, or point at Compose/Atlas Mongo.
MONGODB_URI=
MONGODB_DB=wallbang
SERVER_STATUS_TTL_SECONDS=15
SERVER_STATUS_DOC_TTL_SECONDS=120

# Optional Redis (used when the full Compose stack is running).
# REDIS_URL=redis://127.0.0.1:6379
